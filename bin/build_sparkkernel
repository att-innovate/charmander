#!/bin/bash

set -e

SCRIPT_HOME="$( cd "$( dirname "$0" )" && pwd )"

cd $SCRIPT_HOME/..

for ANALYTICS_NODE in $(cat ./node_analytics.txt)
do
    vagrant ssh -c "sudo /vagrant/bin/invm/build_sparkkernel.sh" $ANALYTICS_NODE
done

cd - > /dev/null
