#!/bin/bash

set -e

SCRIPT_HOME="$( cd "$( dirname "$0" )" && pwd )"
HOST_NAME="master1"

cd $SCRIPT_HOME/..

vagrant ssh -c "sudo /vagrant/bin/deploy_scheduler-inner.sh" $HOST_NAME

cd - > /dev/null
